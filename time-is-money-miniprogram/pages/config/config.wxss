.container { padding: 24rpx; }
.section { background: #1a1a2e; border-radius: 20rpx; padding: 28rpx; margin-bottom: 24rpx; }
.section-header { display: flex; align-items: center; margin-bottom: 24rpx; }
.section-icon { font-size: 36rpx; margin-right: 12rpx; }
.section-title { font-size: 30rpx; font-weight: bold; color: #00d4ff; }

.radio-group { display: flex; gap: 16rpx; margin-bottom: 20rpx; }
.radio-item {
  flex: 1; height: 72rpx; line-height: 72rpx; text-align: center;
  background: #0f0f23; border-radius: 12rpx; font-size: 28rpx;
  color: #8888aa; border: 1px solid transparent;
}
.radio-item.active { background: rgba(0,212,255,0.15); color: #00d4ff; border-color: #00d4ff; }

.input {
  width: 100%; height: 72rpx; background: #0f0f23; border-radius: 12rpx;
  padding: 0 20rpx; font-size: 28rpx; color: #fff; box-sizing: border-box; margin-bottom: 20rpx;
}

.row-fields { display: flex; gap: 20rpx; align-items: center; }
.field-half { flex: 1; display: flex; align-items: center; gap: 12rpx; }
.field-label { font-size: 24rpx; color: #8888aa; white-space: nowrap; }
.field-unit { font-size: 24rpx; color: #8888aa; }
.input-sm {
  flex: 1; height: 64rpx; background: #0f0f23; border-radius: 10rpx;
  padding: 0 16rpx; font-size: 26rpx; color: #fff; box-sizing: border-box; min-width: 0;
}
.picker-sm {
  line-height: 64rpx; overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
}

.save-btn {
  width: 100%; height: 88rpx;
  background: linear-gradient(135deg, #00d4ff, #0077ff);
  border-radius: 44rpx; font-size: 32rpx; border: none; color: #fff;
  display: flex; align-items: center; justify-content: center;
}

/* === Calendar === */
.cal-hint { font-size: 22rpx; color: #8888aa; margin-bottom: 16rpx; display: block; }
.cal-nav { display: flex; justify-content: space-between; align-items: center; margin-bottom: 20rpx; }
.cal-nav-btn { font-size: 36rpx; color: #00d4ff; padding: 8rpx 24rpx; }
.cal-nav-title { font-size: 30rpx; color: #fff; font-weight: bold; }
.cal-weekday-row { display: flex; margin-bottom: 8rpx; }
.cal-weekday-header {
  flex: 1; text-align: center; padding: 12rpx 0; border-radius: 10rpx;
  font-size: 26rpx; font-weight: bold; margin: 0 4rpx;
}
.cal-weekday-header.on { background: rgba(0,212,255,0.2); color: #00d4ff; }
.cal-weekday-header.off { background: rgba(255,126,179,0.15); color: #ff7eb3; }
.cal-week-row { display: flex; margin-bottom: 4rpx; }
.cal-day-cell {
  flex: 1; text-align: center; padding: 16rpx 0; border-radius: 10rpx;
  font-size: 26rpx; margin: 0 4rpx;
}
.cal-day-cell.empty { visibility: hidden; }
.cal-day-cell.work { background: rgba(0,212,255,0.12); color: #00d4ff; }
.cal-day-cell.rest { background: rgba(255,126,179,0.1); color: #ff7eb3; }
.cal-day-cell.today { border: 2rpx solid #ffd700; font-weight: bold; }
